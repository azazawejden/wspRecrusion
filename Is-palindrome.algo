FUNCTION Is-palindrome(word:STRING) : BOOLEAN
VAR
VAR
    word : STRING
    ch-Left : CHAR
    ch-Right : CHAR
    ch-enFIN : CHAR
BEGIN

    write(word)
    read(word)
    IF (ch-Left==ch-Right OR ch-Right==ch-Left) THEN
        IF (ch-enFIN are equality) THEN
            write(we test the rest of the word)
        ELSE
            write(we stop)
        END_IF
    END_IF

    IF (word)== OR word=="single caractere" THEN
        word:=palindrome
    END_IF

    RETURN value ;
END
